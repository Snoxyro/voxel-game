package com.voxelgame.game;

/**
 * Represents a block type. AIR means empty space — no geometry is emitted for it.
 * Additional types will be added as the block registry grows.
 */
public enum Block {
    AIR,
    GRASS
}